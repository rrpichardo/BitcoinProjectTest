# Local defaults for the assignment pipeline.
# Copy to .env only if you want to override the built-in defaults.

KAFKA_BOOTSTRAP=localhost:9092
MODEL_PATH=models/artifacts/lr_pipeline.pkl
MODEL_VERSION=v1.0
